# CLAUDE.md

Follow the guidelines in @AGENTS.md.

Key points: this is Adam's learning project — do not implement ROADMAP.md features unless explicitly asked; analyses return frozen `Result` dataclasses (never print); preprocessing must be leakage-safe; run `pytest` before finishing; keep files UTF-8.
